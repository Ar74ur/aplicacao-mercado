import { api } from "@/services/api";
import { useState } from "react";

interface Product {
  id: number;
  name: string;
  preco: number;
}

export default function Home() {
  return (
    <main>

    </main>
  );
}
